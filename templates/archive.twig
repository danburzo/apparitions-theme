{% extends 'base.twig' %}

{% block content %}
	{% if posts | length > 0 %}
		{% for post in posts %}
			{% include 'components/tease-post.twig' %}
		{% endfor %}
	{% else %}
		{{__('Nu există știri încă', 'apparitions')}}
	{% endif %}

	{% include 'components/pagination-general.twig' %}
{% endblock %}